"""Code-fact extractor namespace ported from cipher-2.

The libclang AST extractor lives under ``.code`` (mirroring cipher-2's
``cipher2.initializer.extractor.code`` package path), so the import path is
``arbiter_engine.facts.extractor.code``.
"""
